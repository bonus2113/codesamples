//The modes all score APIs must support.
public enum ScoreAPIMode {
	//Guest mode means that the user is not logged into the service.
	Guest,
	//In verified mode the user has to be authenticated with the service.
	Verified
}